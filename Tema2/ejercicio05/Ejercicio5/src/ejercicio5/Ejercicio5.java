/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Main.java to edit this template
 */
package ejercicio5;

/**
 *
 * @author Sara
 */
public class Ejercicio5 {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        int mi carta; //Es incorrecto, miCarta.
        int unacarta; //Es incorrecto, unaCarta.
        int Mis2escritos; //Es incorrecto porque la primera letra tiene que ser minúscula, misDosEscritos.
        int 4cientos; //Es incorrecto porque la variable siempre tiene que empezar por una letra.
        int es_un_mensaje; //Es incorrecto, esUnMensaje.
        int no_vale nada; //Es incorrecto, noValeNada.
        int ____ejemplo_____; //Es incorrecto porque la variable siempre tiene que empezar por una letra, ejemplo.
        int mi-programa; //Es incorrecto, miPrograma.
        int ¿cuantos?; //Es incorrecto porque la variable solo puede empezar por una letra, cuantos.
        int el%Descontado; //Es incorrecto, elDescontado.
        int a200PORAÑO; //Es incorrecto, aDoscientosPorAño.
        int TengoMUCHOS$$$; //Es incorrecto porque la primera letra de la variable tiene que estar escrita en minúsuculas, tengoMuchosDineros.
        int LOS400GOLPES; //Es incorrecto porque la primera letra de la variable tiene que estar escrita en minúsuculas, losCuatrocientosGolpes.
        int quieroUNAsolución; //Es incorrecto no puede tener tildes, quieroUnaSolucion.
    }
    
}
